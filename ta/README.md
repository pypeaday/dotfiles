# Tmux Attach

Inspired by Waylon, I used this script for a long time. `uv` is changing the
game along with tools like Claude for automating workflows I otherwise wouldn't
sink hours and hours into. So `ta` is retired for my `workspaces` module now
