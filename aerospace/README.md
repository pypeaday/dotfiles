# AeroSpace

Reference [docs](https://nikitabobko.github.io/AeroSpace/guide#configuring-aerospace) for setup on Mac

`brew install --cask nikitabobko/tap/aerospace`

```
defaults write com.apple.dock expose-group-apps -bool true && killall Dock

defaults write com.apple.spaces spans-displays -bool true && killall SystemUIServer

```

## JankyBorders

brew tap FelixKratz/formulae
brew install borders

## Mac Settings

Need to go in and disable the things that clash with my aerospace config, which are things I never use

Also need to turn off all the Mac Spaces stuff

- all the windows keyboard shortcuts
- all the screenshot default shortcuts
